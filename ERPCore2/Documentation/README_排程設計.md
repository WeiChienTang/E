# 生產排程設計文檔

## 概述

生產排程模組用於將銷貨訂單轉換為生產計劃，追蹤生產進度，並管理訂單與生產的關聯。

## 資料結構

### 現有實體關係圖

```
┌─────────────────┐       ┌─────────────────────┐       ┌──────────────────────────┐
│   SalesOrder    │ 1   n │  SalesOrderDetail   │ 1   n │  ProductionScheduleItem  │
│   (銷貨訂單)     │───────│  (訂單明細)          │───────│  (排程項目)               │
└─────────────────┘       └─────────────────────┘       └──────────────────────────┘
                                   │                              │
                                   │ ScheduledQuantity            │
                                   │ (已排程數量)                  │
                                   │                              │
                                   ▼                              ▼
                          ┌─────────────────────┐       ┌─────────────────────────┐
                          │ PendingScheduleQty  │       │  ProductionSchedule     │
                          │ = OrderQty -        │       │  (排程主檔)              │
                          │   ScheduledQuantity │       └─────────────────────────┘
                          └─────────────────────┘
```

### 實體說明

| 實體 | 說明 | 關鍵欄位 |
|------|------|----------|
| `ProductionSchedule` | 排程主檔（一張排程單）| `Code`, `ScheduleDate`, `CustomerId`, `CreatedByEmployeeId` |
| `ProductionScheduleItem` | 排程項目（每個要生產的商品）| `ProductionScheduleId`, `ProductId`, `SalesOrderDetailId`, `ScheduledQuantity`, `CompletedQuantity` |
| `SalesOrderDetail` | 訂單明細 | `ScheduledQuantity`（已排程數量）, `PendingScheduleQuantity`（待排程數量，計算屬性）|
| `ProductionScheduleAllocation` | 分配記錄（多訂單共用同一生產批次時使用）| `ProductionScheduleItemId`, `SalesOrderDetailId`, `AllocatedQuantity` |

### 數量欄位說明

**SalesOrderDetail:**
- `OrderQuantity`: 訂單數量（客戶訂購的總數）
- `ScheduledQuantity`: 已排程數量（已轉入生產排程的數量）
- `PendingScheduleQuantity`: 待排程數量 = `OrderQuantity - ScheduledQuantity`（計算屬性）

**ProductionScheduleItem:**
- `ScheduledQuantity`: 排程數量（本次排程要生產的數量）
- `CompletedQuantity`: 已完成數量（已入庫的完成品數量）
- `PendingQuantity`: 待完成數量 = `ScheduledQuantity - CompletedQuantity`（計算屬性）

---

## UI 設計

### 設計理念

**以明細/商品為核心（v2.0）**

在製造排程實務中，生產人員通常會將多張訂單中相同的商品合併成一批生產，以提高效率。因此，排程介面採用「以明細/商品為核心」的設計：

- ✅ **直接顯示商品** - 左側表格直接顯示商品資訊，可按商品篩選/分組
- ✅ **單步操作** - 點擊或拖放明細即可加入排程，無需彈窗
- ✅ **同產品一目了然** - 相同商品會排在一起，方便識別可合併生產的項目
- ✅ **快速搜尋** - 可搜尋商品、訂單編號、客戶名稱

### 生產排程編輯介面佈局

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│                              生產排程編輯                                         │
│  ┌─────────────────────────────────────────────────────────────────────────────┐ │
│  │ [排程編號] [排程日期] [客戶▼] [製單人員▼]                                     │ │
│  └─────────────────────────────────────────────────────────────────────────────┘ │
├────────────────────────────────────┬─────────────────────────────────────────────┤
│        待排程明細 (左側)            │            排程項目 (右側)                   │
├────────────────────────────────────┼─────────────────────────────────────────────┤
│ 🔍 搜尋: [商品/訂單/客戶______]   │                                             │
│                                    │                                             │
│ ┌────────────────────────────────┐ │ ┌─────────────────────────────────────────┐ │
│ │ 商品      訂單     客戶   待排數量│ │ │ 商品     來源訂單   排程數量  狀態  操作│ │
│ ├────────────────────────────────┤ │ ├─────────────────────────────────────────┤ │
│ │ 🔗 P001    SO-001  客戶A    100 │ │ │ 商品A   SO-001      100    待處理  [-] │ │
│ │    商品A   01/25                │ │ │ 商品B   SO-001       50    待處理  [-] │ │
│ │ 🔗 P001    SO-003  客戶B     50 │ │ │ 商品C   SO-002       30    待處理  [-] │ │
│ │    商品A   01/24                │ │ └─────────────────────────────────────────┘ │
│ │ 🔗 P002    SO-001  客戶A     30 │ │                                             │
│ │    商品B   01/25                │ │                                             │
│ │ 🔗 P003    SO-002  客戶C     80 │ │                                             │
│ │    商品C   01/23                │ │                                             │
│ └────────────────────────────────┘ │                                             │
│                                    │                                             │
│ * 🔗 可拖曳至右側表格               │ * 支援從左側拖放明細直接加入               │
│ * 點擊即可直接加入排程             │ * 可調整排程數量或移除項目               │
│ * 按商品編號排序，同產品排在一起   │                                             │
└────────────────────────────────────┴─────────────────────────────────────────────┘
```

### 設計比較

| 面向 | 舊版（訂單為主） | 新版（明細為主） |
|------|----------------|----------------|
| **左側顯示** | 訂單主檔 | 訂單明細（商品） |
| **操作流程** | 點擊訂單 → 彈窗選明細 → 加入 | 點擊/拖放明細 → 直接加入 |
| **同產品識別** | 需進入彈窗才能看到 | 直接顯示，同商品排在一起 |
| **操作步驟** | 2-3 步 | 1 步 |

> ℹ️ **註**：`ProductionScheduleDetailSelectorModal` 仍保留在程式碼中，未來如需批量選擇功能可重新啟用。

---

## 拖放功能設計（已實作）

### 架構圖

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              拖放功能架構                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────┐                    ┌─────────────────────┐        │
│  │ DragDropState.cs    │                    │ DragDropEventArgs.cs│        │
│  │ (靜態狀態管理)       │                    │ (事件參數)           │        │
│  ├─────────────────────┤                    ├─────────────────────┤        │
│  │ • DraggedItem       │                    │ • DroppedItem       │        │
│  │ • DragDropGroup     │───────────────────→│ • TargetIndex       │        │
│  │ • DraggedIndex      │                    │ • SourceTableId     │        │
│  │ • SourceTableId     │                    │ • TargetTableId     │        │
│  │ • IsDragging        │                    │ • GetDroppedItem<T> │        │
│  │ • StartDrag()       │                    └─────────────────────┘        │
│  │ • EndDrag()         │                              ▲                    │
│  │ • CanDropTo()       │                              │                    │
│  └─────────────────────┘                              │                    │
│           │                                           │                    │
│           ▼                                           │                    │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │                    InteractiveTableComponent                         │  │
│  ├─────────────────────────────────────────────────────────────────────┤  │
│  │ 拖曳來源參數:                      放置目標參數:                      │  │
│  │ • EnableDrag = true                • EnableDrop = true               │  │
│  │ • DragDropGroup = "production-     • DragDropGroup = "production-    │  │
│  │                   schedule"                        schedule"         │  │
│  │ • TableId = "order-table"          • TableId = "item-table"          │  │
│  │ • OnDragStart                      • OnItemDropped                   │  │
│  │ • IsDraggable                      • DropZoneEmptyTemplate           │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────┐     │
│  │                       drag-drop-helper.js                          │     │
│  ├───────────────────────────────────────────────────────────────────┤     │
│  │ • initDragDrop()             - 初始化拖曳事件                       │     │
│  │ • preventTextSelectionOnDrag() - 防止拖曳時文字選取                 │     │
│  │ • reinitDragDrop()           - Blazor 更新後重新初始化              │     │
│  │ • setCheckboxIndeterminate() - 設定 checkbox 半選狀態              │     │
│  └───────────────────────────────────────────────────────────────────┘     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 拖放流程

```
1. 使用者在左側表格按住明細列
   └── InteractiveTableComponent 觸發 @ondragstart
       └── 呼叫 DragDropState.StartDrag(item, group, index, tableId)
       └── 觸發 OnDragStart 事件

2. 使用者拖曳到右側表格上方
   └── InteractiveTableComponent 觸發 @ondragover
       └── 呼叫 DragDropState.CanDropTo(group) 檢查是否可放置
       └── 顯示視覺提示（DropZoneEmptyTemplate 或行高亮）

3. 使用者放開滑鼠（drop）
   └── InteractiveTableComponent 觸發 @ondrop
       └── 建立 DragDropEventArgs 並觸發 OnItemDropped 事件
       └── 呼叫 DragDropState.EndDrag() 清除狀態

4. ProductionScheduleItemTable 處理 OnItemDropped
   └── 從 DragDropEventArgs.GetDroppedItem<PendingScheduleDetailDto>() 取得明細
   └── 觸發 OnDetailDropped 事件通知父組件

5. ProductionScheduleEditModalComponent 處理 OnDetailDropped
   └── 呼叫 AddDetailToSchedule(detail) 直接加入排程項目列表
```

---

## 選取欄位功能（InteractiveTableComponent 新增功能）

### 功能說明

為 `InteractiveTableComponent` 新增了獨立的選取欄位（ShowSelectColumn），支援：

1. **全選/全不選 Checkbox** - 表頭顯示，可一次選取/取消所有可選項目
2. **半選狀態（Indeterminate）** - 當部分項目被選取時顯示
3. **可選取判斷** - 透過 `IsItemSelectable` 函數判斷特定項目是否可選
4. **不可選取替代內容** - 透過 `UnselectableTemplate` 顯示自訂內容（如完成圖示）

### 相關參數

```csharp
// InteractiveTableComponent 選取欄位參數
[Parameter] public bool ShowSelectColumn { get; set; } = false;
[Parameter] public string SelectColumnWidth { get; set; } = "50px";
[Parameter] public Func<TItem, bool>? IsItemSelectable { get; set; }
[Parameter] public RenderFragment<TItem>? UnselectableTemplate { get; set; }
[Parameter] public bool AllowMultipleSelection { get; set; } = false;
[Parameter] public HashSet<TItem>? SelectedItems { get; set; }
[Parameter] public EventCallback<HashSet<TItem>> OnSelectionChanged { get; set; }
```

### 使用範例（ProductionScheduleDetailSelectorModal）

```razor
<InteractiveTableComponent TItem="PendingScheduleDetailDto"
                           Items="@Details"
                           ShowSelectColumn="true"
                           AllowMultipleSelection="true"
                           SelectedItems="@_selectedItems"
                           OnSelectionChanged="HandleSelectionChanged"
                           IsItemSelectable="@(d => d.PendingQuantity > 0)"
                           UnselectableTemplate="@GetUnselectableTemplate()" />
```

---

## 操作流程

### 新增排程流程

```
1. 開啟生產排程編輯 Modal（新增模式）
   ├── 系統自動產生排程編號
   └── 左側載入所有待排程明細（以商品為核心顯示）

2. 使用者填寫主檔資訊
   ├── 排程日期（預設今天）
   ├── 客戶（可選）
   └── 製單人員

3. 從左側選擇明細（兩種方式，都是單步操作）
   │
   ├── 方式一：點擊明細列
   │   └── 直接加入右側排程項目列表
   │
   └── 方式二：拖放明細到右側表格
       └── 放開後直接加入右側排程項目列表

4. 右側顯示已選的排程項目
   ├── 可調整排程數量
   └── 可點擊 [-] 移除項目

5. 儲存排程
   ├── 建立 ProductionSchedule 主檔
   ├── 建立 ProductionScheduleItem 項目
   └── 更新 SalesOrderDetail.ScheduledQuantity
```

### 編輯排程流程

```
1. 開啟生產排程編輯 Modal（編輯模式）
   ├── 載入排程主檔資訊
   └── 右側載入已存在的排程項目

2. 可執行的操作
   ├── 修改主檔資訊
   ├── 調整現有項目的排程數量
   ├── 從左側加入更多項目（點擊或拖放）
   └── 移除現有項目

3. 儲存時
   ├── 更新主檔
   ├── 更新/新增/刪除項目
   └── 重新計算 SalesOrderDetail.ScheduledQuantity
```

---

## 組件設計

### 檔案結構

```
Components/Pages/ProductionManagement/
├── ProductionScheduleEditModalComponent.razor    # 主編輯 Modal（整合所有子組件）
├── ProductionScheduleOrderTable.razor            # 左側：待排程訂單表格（拖曳來源）
├── ProductionScheduleItemTable.razor             # 右側：排程項目表格（放置目標）
└── ProductionScheduleDetailSelectorModal.razor   # 明細選擇彈窗（使用選取欄位功能）

Components/Shared/Base/
└── InteractiveTableComponent.razor               # 通用表格（含拖放和選取欄位功能）

Helpers/
├── DragDropState.cs                              # 拖放狀態管理（靜態類別）
└── DragDropEventArgs.cs                          # 拖放事件參數

wwwroot/js/
└── drag-drop-helper.js                           # 拖放 JavaScript 輔助函數
```

### 組件職責

| 組件 | 職責 |
|------|------|
| `ProductionScheduleEditModalComponent` | 主控制器，管理狀態、協調子組件、處理儲存 |
| `ProductionScheduleOrderTable` | 顯示待排程明細列表（以商品為核心），提供搜尋功能，支援拖曳（EnableDrag） |
| `ProductionScheduleItemTable` | 顯示/編輯排程項目，支援數量調整、刪除和拖放接收（EnableDrop） |
| `ProductionScheduleDetailSelectorModal` | （已棄用）原用於顯示訂單明細選擇，現已不需要 |

### 組件通訊

```
ProductionScheduleEditModalComponent
    │
    ├── ProductionScheduleOrderTable
    │       │
    │       ├── OnDetailSelected(PendingScheduleDetailDto detail)  ──→  直接加入排程項目
    │       └── OnDragStart(TItem item)  ──→  開始拖曳
    │
    └── ProductionScheduleItemTable
            │
            ├── OnDetailDropped(PendingScheduleDetailDto detail)  ──→  直接加入排程項目
            ├── OnItemRemoved(ScheduleItemDto item)  ──→  從列表移除
            └── OnQuantityChanged(item, newQuantity)  ──→  更新數量
```

---

## 資料傳輸物件 (DTO)

### PendingScheduleOrderDto（待排程訂單 - 已棄用）

> ⚠️ **已棄用**：此 DTO 在 v2.0 以明細為核心的設計中已不再使用，保留供參考。

```csharp
public class PendingScheduleOrderDto
{
    public int SalesOrderId { get; set; }
    public string OrderCode { get; set; } = "";
    public DateTime OrderDate { get; set; }
    public int? CustomerId { get; set; }
    public string CustomerName { get; set; } = "";
    public int PendingItemCount { get; set; };        // 待排程項目數
    public decimal TotalPendingQuantity { get; set; }; // 待排程總數量
    public List<PendingScheduleDetailDto> Details { get; set; } = new(); // 明細列表
}
```

### PendingScheduleDetailDto（待排程明細）

```csharp
public class PendingScheduleDetailDto
{
    public int SalesOrderDetailId { get; set; }
    
    // 訂單相關欄位（新增）
    public int SalesOrderId { get; set; }           // 來源訂單 ID
    public string SalesOrderCode { get; set; } = ""; // 來源訂單編號
    public DateTime OrderDate { get; set; }          // 訂單日期
    public int CustomerId { get; set; }              // 客戶 ID
    public string CustomerName { get; set; } = "";   // 客戶名稱
    
    // 商品相關欄位
    public int ProductId { get; set; }
    public string ProductCode { get; set; } = "";
    public string ProductName { get; set; } = "";
    
    // 數量相關欄位
    public decimal OrderQuantity { get; set; }
    public decimal ScheduledQuantity { get; set; }
    public decimal PendingQuantity { get; set; }      // 可排程數量
    public decimal ScheduleQuantity { get; set; }     // 本次排程數量
    public bool IsSelected { get; set; }              // 是否被選取
}
```

### ScheduleItemDto（排程項目）

```csharp
public class ScheduleItemDto
{
    public int? Id { get; set; }                    // 如果是編輯現有項目
    public int SalesOrderId { get; set; }           // 來源訂單 ID
    public string SalesOrderCode { get; set; } = ""; // 來源訂單編號
    public int SalesOrderDetailId { get; set; }     // 來源訂單明細 ID
    public int ProductId { get; set; }              // 商品 ID
    public string ProductCode { get; set; } = "";   // 商品編號
    public string ProductName { get; set; } = "";   // 商品名稱
    public string CustomerName { get; set; } = "";  // 客戶名稱
    public decimal ScheduleQuantity { get; set; }   // 排程數量
    public decimal PendingScheduleQuantity { get; set; } // 剩餘可排程數量
    public ProductionItemStatus Status { get; set; } // 狀態
    public bool IsNew { get; set; }                 // 是否為新項目
    public bool CanDelete { get; set; }             // 是否可刪除
}
```

---

## Service 需求

### ISalesOrderService（需新增）

```csharp
/// <summary>
/// 取得有待排程項目的訂單列表
/// </summary>
Task<List<SalesOrder>> GetOrdersWithPendingScheduleAsync(int? customerId = null);
```

### ISalesOrderDetailService（需新增）

```csharp
/// <summary>
/// 取得指定訂單的待排程明細
/// </summary>
Task<List<SalesOrderDetail>> GetPendingScheduleDetailsByOrderIdAsync(int salesOrderId);

/// <summary>
/// 更新明細的已排程數量
/// </summary>
Task UpdateScheduledQuantityAsync(int detailId, decimal scheduledQuantity);
```

### IProductionScheduleService（需檢查/新增）

```csharp
/// <summary>
/// 儲存排程（含項目）
/// </summary>
Task<ServiceResult<ProductionSchedule>> SaveScheduleWithItemsAsync(
    ProductionSchedule schedule, 
    List<ScheduleItemDto> items);
```

---

## 業務規則

### 數量驗證

1. **本次排程數量** ≤ **待排程數量**
2. **本次排程數量** > 0
3. 儲存時自動更新 `SalesOrderDetail.ScheduledQuantity`

### 狀態控制

1. 訂單明細 `PendingScheduleQuantity = 0` 時，該明細不可再排程
2. 訂單所有明細都已排完時，左側不顯示該訂單
3. 排程項目 `CompletedQuantity > 0` 時，該項目不可刪除（已有生產紀錄）

### 刪除/移除規則

1. 移除排程項目時，需還原 `SalesOrderDetail.ScheduledQuantity`
2. 刪除整張排程時，需還原所有相關明細的 `ScheduledQuantity`

---

## 實作順序

### 第一階段：基礎架構 ✅ 已完成
1. [x] 建立 `ProductionScheduleOrderTable.razor`
2. [x] 建立 `ProductionScheduleItemTable.razor`
3. [x] 建立 `ProductionScheduleDetailSelectorModal.razor`
4. [x] 修改 `ProductionScheduleEditModalComponent.razor` 整合組件

### 第二階段：拖放功能 ✅ 已完成
5. [x] 建立 `DragDropState.cs` 靜態狀態管理
6. [x] 建立 `DragDropEventArgs.cs` 事件參數
7. [x] 建立 `drag-drop-helper.js` JavaScript 輔助函數
8. [x] 修改 `InteractiveTableComponent.razor` 加入拖放參數
9. [x] 整合拖放功能到 OrderTable 和 ItemTable

### 第三階段：選取欄位功能 ✅ 已完成
10. [x] 為 `InteractiveTableComponent` 新增 ShowSelectColumn 參數
11. [x] 實作全選/全不選 Checkbox（含半選狀態）
12. [x] 實作 IsItemSelectable 和 UnselectableTemplate
13. [x] 重構 `ProductionScheduleDetailSelectorModal` 使用選取欄位功能
14. [x] 使用 `BaseModalComponent` 處理 Modal z-index（需設定 ShowFooter="true"）

### 第四階段：Service 層
15. [ ] 新增 `GetOrdersWithPendingScheduleAsync` 方法
16. [ ] 新增 `GetPendingScheduleDetailsByOrderIdAsync` 方法
17. [ ] 實作 `SaveScheduleWithItemsAsync` 方法

### 第五階段：整合測試
18. [ ] 測試新增排程流程（點擊按鈕）
19. [ ] 測試新增排程流程（拖放）
20. [ ] 測試編輯排程流程
21. [ ] 測試數量計算正確性

---

## 已知問題與解決方案

### 問題 1：BaseModalComponent Footer 不顯示
**原因**：`BaseModalComponent` 預設 `ShowFooter="false"`，導致 `FooterContent` 區塊不會渲染。

**解決方案**：在使用 `BaseModalComponent` 時，若需要顯示 Footer，必須明確設定 `ShowFooter="true"`。

```razor
<BaseModalComponent IsVisible="@IsVisible"
                    ShowFooter="true"
                    ...>
    <FooterContent>
        <!-- Footer 內容 -->
    </FooterContent>
</BaseModalComponent>
```

---

## 變更記錄

| 日期 | 變更內容 | 負責人 |
|------|----------|--------|
| 2026-01-26 | 初版設計文檔 | - |
| 2026-01-26 | 實作拖放功能（DragDropState、DragDropEventArgs、drag-drop-helper.js） | - |
| 2026-01-26 | InteractiveTableComponent 新增 EnableDrag、EnableDrop、DragDropGroup、TableId 等參數 | - |
| 2026-01-26 | InteractiveTableComponent 新增 ShowSelectColumn、IsItemSelectable、UnselectableTemplate 參數 | - |
| 2026-01-26 | 重構 ProductionScheduleDetailSelectorModal 使用 InteractiveTableComponent 選取欄位功能 | - |
| 2026-01-26 | 修正 BaseModalComponent ShowFooter 參數，確保 Footer 正確顯示確認按鈕 | - |
| 2026-01-27 | **v2.0 重構：改為以明細/商品為核心的設計** | - |
| 2026-01-27 | 更新 PendingScheduleDetailDto，新增訂單相關欄位（SalesOrderId, SalesOrderCode, OrderDate, CustomerId, CustomerName） | - |
| 2026-01-27 | 重構 ProductionScheduleOrderTable，從顯示訂單主檔改為顯示明細（以商品為核心） | - |
| 2026-01-27 | 更新 ProductionScheduleItemTable，將 OnOrderDropped 改為 OnDetailDropped | - |
| 2026-01-27 | 更新 ProductionScheduleEditModalComponent，移除彈窗選擇邏輯，改為直接處理明細 | - |
| 2026-01-27 | 簡化操作流程：點擊或拖放明細即可直接加入排程，無需彈窗 | - |
