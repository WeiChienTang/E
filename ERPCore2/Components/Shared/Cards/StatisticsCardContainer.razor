@using ERPCore2.Components.Shared.Cards

@* 統計卡片容器元件 *@

<div class="row mb-4">
    @ChildContent
</div>

@code {
    /// <summary>
    /// 子內容 (通常是 StatisticsCard 元件)
    /// </summary>
    [Parameter] 
    public RenderFragment? ChildContent { get; set; }
}
