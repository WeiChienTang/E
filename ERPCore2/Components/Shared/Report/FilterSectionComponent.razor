@* 
    統一的篩選區塊容器組件
    用於批次列印或其他需要多個篩選條件的場景
    提供一致的樣式、結構和間距
*@

<div class="filter-section-body">
    @ChildContent
</div>

<style>
    .filter-section-body {
        padding: 0.5rem 0;
    }
</style>

@code {
    /// <summary>
    /// 區塊標題（選填，不再顯示但保留以維持向後相容）
    /// </summary>
    [Parameter]
    public string? Title { get; set; }

    /// <summary>
    /// 標題前的圖示 CSS 類別（選填，不再顯示但保留以維持向後相容）
    /// </summary>
    [Parameter]
    public string? IconClass { get; set; }

    /// <summary>
    /// 標題後的徽章文字（選填，不再顯示但保留以維持向後相容）
    /// </summary>
    [Parameter]
    public string? Badge { get; set; }

    /// <summary>
    /// 額外的 CSS 類別（不再使用但保留以維持向後相容）
    /// </summary>
    [Parameter]
    public string CssClass { get; set; } = "";

    /// <summary>
    /// 標題區域的額外操作按鈕（選填，不再顯示但保留以維持向後相容）
    /// </summary>
    [Parameter]
    public RenderFragment? HeaderActions { get; set; }

    /// <summary>
    /// 區塊內容
    /// </summary>
    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}
