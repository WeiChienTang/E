@* 客戶銀行帳號編輯組件 *@
@inject ICustomerBankAccountService BankAccountService
@inject IBankService BankService
@inject INotificationService NotificationService
@inject IStringLocalizer<SharedResource> L
@using ERPCore2.Models.FeatureGuides.GuideDefinitions

<GenericEditModalComponent TEntity="CustomerBankAccount"
                          TService="ICustomerBankAccountService"
                          @ref="editModalComponent"
                          IsVisible="@IsVisible"
                          IsVisibleChanged="@IsVisibleChanged"
                          @bind-Id="@BankAccountId"
                          Service="@BankAccountService"
                          EntityName="@L["Entity.CustomerBankAccount"]"
                          EntityNamePlural="@L["Entity.CustomerBankAccount"]"
                          ModalTitle="@(BankAccountId.HasValue ? L["Message.EditTitle", L["Entity.CustomerBankAccount"]].ToString() : L["Message.CreateTitle", L["Entity.CustomerBankAccount"]].ToString())"
                          Size="GenericEditModalComponent<CustomerBankAccount, ICustomerBankAccountService>.ModalSize.Default"
                          UseGenericForm="true"
                          FormFields="@formFields"
                          FormSections="@formSections"
                          DataLoader="@LoadData"
                          AdditionalDataLoader="@LoadAdditionalDataAsync"
                          UseGenericSave="true"
                          SaveSuccessMessage="@(BankAccountId.HasValue ? L["Message.UpdateSuccess", L["Entity.CustomerBankAccount"]].ToString() : L["Message.CreateSuccess", L["Entity.CustomerBankAccount"]].ToString())"
                          SaveFailureMessage="@L["Message.SaveFailure", L["Entity.CustomerBankAccount"]]"
                          RequiredPermission="@PermissionRegistry.Customer.Read"
                          OnEntitySaved="@OnSaved"
                          OnCancel="@OnCancel"
                          ComponentName="CustomerBankAccountEditModalComponent"
                          FeatureGuide="@CustomerBankAccountGuide.Create()" />

@code {
    [Parameter] public bool IsVisible { get; set; } = false;
    [Parameter] public EventCallback<bool> IsVisibleChanged { get; set; }
    [Parameter] public int? BankAccountId { get; set; }
    [Parameter] public EventCallback<CustomerBankAccount> OnSaved { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }
    [Parameter] public int? PrefilledCustomerId { get; set; }

    private GenericEditModalComponent<CustomerBankAccount, ICustomerBankAccountService>? editModalComponent;
    private List<FormFieldDefinition> formFields = new();
    private Dictionary<string, string> formSections = new();

    private List<Bank> availableBanks = new();

    private Task<CustomerBankAccount?> LoadData()
    {
        var prefilled = new Dictionary<string, object?>();
        if (PrefilledCustomerId.HasValue)
            prefilled[nameof(CustomerBankAccount.CustomerId)] = PrefilledCustomerId.Value;

        return EditDataLoaderHelper.LoadOrCreateAsync<CustomerBankAccount, ICustomerBankAccountService>(
            BankAccountId, BankAccountService, NotificationService, L["Entity.CustomerBankAccount"].ToString(), string.Empty,
            prefilled.Count > 0 ? prefilled : null,
            b => b.IsPrimary = true);
    }

    private async Task LoadAdditionalDataAsync()
    {
        try
        {
            availableBanks = await BankService.GetAllAsync();
            InitializeFormFields();
            StateHasChanged();
        }
        catch (Exception ex)
        {
            await ErrorHandlingHelper.HandlePageErrorAsync(ex, nameof(LoadAdditionalDataAsync), GetType());
            await NotificationService.ShowErrorAsync(L["Message.LoadFailed", L["Field.Bank"]].ToString());
        }
    }

    protected override void OnParametersSet()
    {
        if (IsVisible)
            InitializeFormFields();
    }

    private void InitializeFormFields()
    {
        try
        {
            formFields = new List<FormFieldDefinition>
            {
                new()
                {
                    PropertyName = nameof(CustomerBankAccount.BankId),
                    Label = L["Field.Bank"],
                    FieldType = FormFieldType.Select,
                    IsRequired = true,
                    Options = availableBanks.Select(b => new SelectOption
                    {
                        Value = b.Id.ToString(),
                        Text = b.BankName
                    }).Prepend(new SelectOption { Value = "", Text = $"-- {L["Label.NotSet"]} --" }).ToList()
                },
                new()
                {
                    PropertyName = nameof(CustomerBankAccount.BranchCode),
                    Label = L["Field.BranchCode"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.BranchCode"]],
                    MaxLength = 10
                },
                new()
                {
                    PropertyName = nameof(CustomerBankAccount.BranchName),
                    Label = L["Field.BranchName"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.BranchName"]],
                    MaxLength = 50
                },
                new()
                {
                    PropertyName = nameof(CustomerBankAccount.AccountNumber),
                    Label = L["Field.AccountNumber"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.AccountNumber"]],
                    IsRequired = true,
                    MaxLength = 30,
                    AutoCompleteAttribute = "off"
                },
                new()
                {
                    PropertyName = nameof(CustomerBankAccount.AccountName),
                    Label = L["Field.BankAccountHolder"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.BankAccountHolder"]],
                    IsRequired = true,
                    MaxLength = 50
                },
                new()
                {
                    PropertyName = nameof(CustomerBankAccount.IsPrimary),
                    Label = L["Field.IsPrimary"],
                    FieldType = FormFieldType.Checkbox,
                    HelpText = L["Hint.OnePrimaryPerCustomer"].ToString()
                },
                FormFieldConfigurationHelper.CreateRemarksField<CustomerBankAccount>()
            };

            formSections = FormSectionHelper<CustomerBankAccount>.Create()
                .AddToSection(FormSectionNames.BasicInfo,
                    b => b.BankId,
                    b => b.BranchCode,
                    b => b.BranchName,
                    b => b.AccountNumber,
                    b => b.AccountName,
                    b => b.IsPrimary)
                .AddToSection(FormSectionNames.AdditionalData,
                    b => b.Remarks)
                .Build();
        }
        catch (Exception)
        {
            _ = NotificationService.ShowErrorAsync("初始化銀行帳號表單欄位時發生錯誤");
        }
    }
}
