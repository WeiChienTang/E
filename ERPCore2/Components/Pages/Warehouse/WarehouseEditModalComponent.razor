@* 可重用的倉庫編輯組件 - 可在任何頁面中嵌入 *@
@inject IWarehouseService WarehouseService
@inject IWarehouseLocationService WarehouseLocationService
@inject INotificationService NotificationService

<GenericEditModalComponent TEntity="Warehouse"
                          TService="IWarehouseService"
                          @ref="editModalComponent"
                          IsVisible="@IsVisible"
                          IsVisibleChanged="@IsVisibleChanged"
                          @bind-Id="@WarehouseId"
                          Service="@WarehouseService"
                          EntityName="倉庫"
                          EntityNamePlural="倉庫"
                          ModalTitle="@(WarehouseId.HasValue ? "編輯倉庫" : "新增倉庫")"
                          Size="GenericEditModalComponent<Warehouse, IWarehouseService>.ModalSize.Desktop"
                          UseGenericForm="true"
                          FormFields="@GetFormFields()"
                          FormSections="@formSections"
                          DataLoader="@LoadWarehouseData"
                          AdditionalDataLoader="@LoadAdditionalDataAsync"
                          UseGenericSave="true"
                          SaveSuccessMessage="@(WarehouseId.HasValue ? "倉庫更新成功" : "倉庫新增成功")"
                          SaveFailureMessage="倉庫儲存失敗"
                          RequiredPermission="Warehouse.Read"
                          OnSaveSuccess="@HandleSaveSuccess"
                          OnCancel="@HandleCancel"
                          OnEntityLoaded="@HandleEntityLoaded"
                          CustomModules="@GetCustomModules()" />

@code {
    // ===== 必要參數 =====
    [Parameter] public bool IsVisible { get; set; } = false;
    [Parameter] public EventCallback<bool> IsVisibleChanged { get; set; }
    [Parameter] public int? WarehouseId { get; set; }
    [Parameter] public EventCallback<Warehouse> OnWarehouseSaved { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }

    // ===== 內部狀態 =====
    private GenericEditModalComponent<Warehouse, IWarehouseService>? editModalComponent;
    private List<FormFieldDefinition> formFields = new();
    private Dictionary<string, string> formSections = new();

    // ===== 庫位資料 =====
    private List<WarehouseLocation> warehouseLocations = new();

    // ===== 必要方法 =====

    /// <summary>
    /// 處理實體載入完成事件（由 GenericEditModalComponent 的導航觸發）
    /// 當上下筆切換時觸發 UI 更新，確保表單欄位正確渲染
    /// </summary>
    private async Task HandleEntityLoaded(int loadedEntityId)
    {
        try
        {
            // 重新載入庫位資料
            await LoadWarehouseLocationsAsync(loadedEntityId);
            StateHasChanged();
        }
        catch (Exception ex)
        {
            await NotificationService.ShowErrorAsync($"載入資料時發生錯誤：{ex.Message}");
        }
    }

    private async Task HandleSaveSuccess()
    {
        try
        {
            if (OnWarehouseSaved.HasDelegate && editModalComponent?.Entity != null)
            {
                // 如果有指定儲存成功回調，則執行
                await OnWarehouseSaved.InvokeAsync(editModalComponent.Entity);
            }
            // CloseModal 已由 GenericEditModalComponent 統一控制
        }
        catch (Exception ex)
        {
            await NotificationService.ShowErrorAsync($"處理儲存成功事件時發生錯誤：{ex.Message}");
        }
    }

    private async Task HandleCancel()
    {
        try
        {
            if (OnCancel.HasDelegate)
            {
                await OnCancel.InvokeAsync();
            }
            await CloseModal();
        }
        catch (Exception ex)
        {
            await NotificationService.ShowErrorAsync($"處理取消事件時發生錯誤：{ex.Message}");
        }
    }

    private async Task CloseModal()
    {
        try
        {
            if (IsVisibleChanged.HasDelegate)
            {
                await IsVisibleChanged.InvokeAsync(false);
            }
        }
        catch (Exception ex)
        {
            await NotificationService.ShowErrorAsync($"關閉視窗時發生錯誤：{ex.Message}");
        }
    }

    private async Task<Warehouse?> LoadWarehouseData()
    {
        try
        {
            if (!WarehouseId.HasValue)
            {
                // 新增模式 - 自動產生倉庫編號，清空庫位資料
                warehouseLocations.Clear();
                var generatedCode = await EntityCodeGenerationHelper.GenerateForEntity<Warehouse, IWarehouseService>(WarehouseService, "WH");
                return new Warehouse
                {
                    Code = generatedCode,
                    Status = EntityStatus.Active
                };
            }

            // 編輯模式 - 載入庫位資料
            await LoadWarehouseLocationsAsync(WarehouseId.Value);
            return await WarehouseService.GetByIdAsync(WarehouseId.Value);
        }
        catch (Exception ex)
        {
            await NotificationService.ShowErrorAsync($"載入倉庫資料時發生錯誤：{ex.Message}");
            return null;
        }
    }

    /// <summary>
    /// 載入倉庫的庫位資料
    /// </summary>
    private async Task LoadWarehouseLocationsAsync(int warehouseId)
    {
        try
        {
            var result = await WarehouseLocationService.GetByWarehouseIdAsync(warehouseId);
            if (result.IsSuccess && result.Data != null)
            {
                warehouseLocations = result.Data.ToList();
            }
            else
            {
                warehouseLocations.Clear();
            }
        }
        catch (Exception ex)
        {
            await NotificationService.ShowErrorAsync($"載入庫位資料時發生錯誤：{ex.Message}");
            warehouseLocations.Clear();
        }
    }

    private async Task LoadAdditionalDataAsync()
    {
        try
        {
            // 庫位資料已在 LoadWarehouseData 中載入
            await Task.CompletedTask;
        }
        catch (Exception ex)
        {
            await NotificationService.ShowErrorAsync($"載入額外資料時發生錯誤：{ex.Message}");
        }
    }

    protected override void OnParametersSet()
    {
        if (IsVisible)
        {
            InitializeFormFields();
        }
    }

    private void InitializeFormFields()
    {
        try
        {
            formFields = new List<FormFieldDefinition>
            {
                new()
                {
                    PropertyName = nameof(Warehouse.Code),
                    Label = "倉庫編號",
                    FieldType = FormFieldType.Text,
                    Placeholder = "請輸入倉庫編號",
                    IsRequired = true,
                    MaxLength = 20,
                    HelpText = "倉庫的唯一識別編號，新增時系統會自動產生，也可手動修改"
                },
                new()
                {
                    PropertyName = nameof(Warehouse.Name),
                    Label = "倉庫名稱",
                    FieldType = FormFieldType.Text,
                    Placeholder = "請輸入倉庫名稱",
                    IsRequired = true,
                    MaxLength = 50,
                    HelpText = "倉庫的唯一名稱，用於識別不同倉庫"
                },
                new()
                {
                    PropertyName = nameof(Warehouse.ContactPerson),
                    Label = "聯絡人",
                    FieldType = FormFieldType.Text,
                    Placeholder = "請輸入聯絡人姓名",
                    IsRequired = false,
                    MaxLength = 50,
                    HelpText = "倉庫的聯絡人姓名"
                },
                new()
                {
                    PropertyName = nameof(Warehouse.Phone),
                    Label = "聯絡電話",
                    FieldType = FormFieldType.Text,
                    Placeholder = "請輸入聯絡電話",
                    IsRequired = false,
                    MaxLength = 20,
                    HelpText = "倉庫的聯絡電話"
                },
                new()
                {
                    PropertyName = nameof(Warehouse.Address),
                    Label = "地址",
                    FieldType = FormFieldType.Text,
                    Placeholder = "請輸入倉庫地址",
                    IsRequired = false,
                    MaxLength = 200,
                    HelpText = "倉庫的詳細地址",
                    ContainerCssClass = "col-4"
                },

                FormFieldConfigurationHelper.CreateRemarksField<Warehouse>()
            };

            formSections = FormSectionHelper<Warehouse>.Create()
                .AddToSection(FormSectionNames.BasicInfo,
                    w => w.Code,
                    w => w.Name,
                    w => w.ContactPerson,
                    w => w.Phone,
                    w => w.Address)
                .AddToSection(FormSectionNames.AdditionalData,
                    w => w.Remarks)
                .Build();
        }
        catch (Exception ex)
        {
            _ = ErrorHandlingHelper.HandlePageErrorAsync(
                ex,
                nameof(InitializeFormFields),
                GetType(),
                additionalData: new { WarehouseId }
            );
        }
    }

    private List<FormFieldDefinition> GetFormFields()
    {
        return formFields;
    }

    /// <summary>
    /// 配置自訂模組 - 庫位表格
    /// </summary>
    private List<GenericEditModalComponent<Warehouse, IWarehouseService>.CustomModule> GetCustomModules()
    {
        return new List<GenericEditModalComponent<Warehouse, IWarehouseService>.CustomModule>
        {
            new GenericEditModalComponent<Warehouse, IWarehouseService>.CustomModule
            {
                Order = 1,
                IsVisible = WarehouseId.HasValue, // 只有編輯模式才顯示
                Content = CreateWarehouseLocationTableContent()
            }
        };
    }

    /// <summary>
    /// 創建庫位表格內容的 RenderFragment
    /// </summary>
    private RenderFragment CreateWarehouseLocationTableContent() => __builder =>
    {
        @if (editModalComponent?.Entity != null && WarehouseId.HasValue)
        {
            <WarehouseLocationTable TMainEntity="Warehouse"
                                   TDetailEntity="WarehouseLocation"
                                   Locations="@warehouseLocations" />
        }
    };
}
