@* 可重用的付款方式編輯組件 - 可在任何頁面中嵌入 *@
@inject IPaymentMethodService PaymentMethodService
@inject INotificationService NotificationService
@inject IStringLocalizer<SharedResource> L

<GenericEditModalComponent TEntity="PaymentMethod"
                          TService="IPaymentMethodService"
                          @ref="editModalComponent"
                          IsVisible="@IsVisible"
                          IsVisibleChanged="@IsVisibleChanged"
                          @bind-Id="@PaymentMethodId"
                          Service="@PaymentMethodService"
                          EntityName="@L["Entity.PaymentMethod"]"
                          EntityNamePlural="@L["Entity.PaymentMethod"]"
                          ModalTitle="@(PaymentMethodId.HasValue ? L["Message.EditTitle", L["Entity.PaymentMethod"]].ToString() : L["Message.CreateTitle", L["Entity.PaymentMethod"]].ToString())"
                          Size="GenericEditModalComponent<PaymentMethod, IPaymentMethodService>.ModalSize.Desktop"
                          UseGenericForm="true"
                          FormFields="@formFields"
                          FormSections="@formSections"
                          DataLoader="@LoadPaymentMethodData"
                          UseGenericSave="true"
                          SaveSuccessMessage="@(PaymentMethodId.HasValue ? L["Message.UpdateSuccess", L["Entity.PaymentMethod"]].ToString() : L["Message.CreateSuccess", L["Entity.PaymentMethod"]].ToString())"
                          SaveFailureMessage="@L["Message.SaveFailure", L["Entity.PaymentMethod"]]"
                          RequiredPermission="@PermissionRegistry.PaymentMethod.Read"
                          OnEntitySaved="@OnPaymentMethodSaved"
                          OnCancel="@OnCancel" />

@code {
    // ===== 參數 =====
    [Parameter] public bool IsVisible { get; set; } = false;
    [Parameter] public EventCallback<bool> IsVisibleChanged { get; set; }
    [Parameter] public int? PaymentMethodId { get; set; }
    [Parameter] public EventCallback<PaymentMethod> OnPaymentMethodSaved { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }
    [Parameter] public Dictionary<string, object?>? PrefilledValues { get; set; }

    // ===== 內部狀態 =====
    private GenericEditModalComponent<PaymentMethod, IPaymentMethodService>? editModalComponent;
    private List<FormFieldDefinition> formFields = new();
    private Dictionary<string, string> formSections = new();

    // ===== 生命週期 =====
    private Task<PaymentMethod?> LoadPaymentMethodData()
        => EditDataLoaderHelper.LoadOrCreateAsync<PaymentMethod, IPaymentMethodService>(
            PaymentMethodId, PaymentMethodService, NotificationService, "付款方式", "PAY", PrefilledValues,
            p => { p.Name = string.Empty; p.IsDefault = false; });
        protected override void OnParametersSet()
    {
        if (IsVisible)
        {
            InitializeFormFields();
        }
    }

    // ===== 表單初始化 =====
    private void InitializeFormFields()
    {
        try
        {
            formFields = new List<FormFieldDefinition>
            {
                new()
                {
                    PropertyName = nameof(PaymentMethod.Code),
                    Label = L["Field.PaymentMethodCode"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.PaymentMethodCode"]],
                    IsRequired = true,
                    HelpText = "付款方式的唯一識別編號，新增時系統會自動產生，也可手動修改"
                },
                // 基本資訊區段
                new()
                {
                    PropertyName = nameof(PaymentMethod.Name),
                    Label = L["Field.PaymentMethodName"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.PaymentMethodName"]],
                    IsRequired = true,
                    HelpText = "付款方式的名稱，例如：現金、信用卡、轉帳等",
                    MaxLength = 50
                },

                FormFieldConfigurationHelper.CreateRemarksField<PaymentMethod>()
            };

            formSections = FormSectionHelper<PaymentMethod>.Create()
                .AddToSection(FormSectionNames.BasicInfo,
                    pm => pm.Code,
                    pm => pm.Name)
                .AddToSection(FormSectionNames.OtherInfo,
                    pm => pm.Remarks)
                .Build();
        }
        catch (Exception ex)
        {
            _ = Task.Run(async () =>
            {
                try
                {
                    await ErrorHandlingHelper.HandlePageErrorAsync(ex, nameof(InitializeFormFields), GetType(), additionalData: "初始化表單欄位失敗");
                    await NotificationService.ShowErrorAsync("初始化表單欄位時發生錯誤");
                }
                catch
                {
                    // 避免錯誤處理本身產生例外
                }
            });
        }
    }

    // ===== 公開方法 (供父組件調用) =====
    public Task ShowAddModal() => editModalComponent!.ShowAddModal();
    public Task ShowEditModal(int id) => editModalComponent!.ShowEditModal(id);
}
