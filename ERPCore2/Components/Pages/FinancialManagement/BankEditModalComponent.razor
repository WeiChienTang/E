@* 可重用的銀行編輯組件 - 可在任何頁面中嵌入 *@
@inject IBankService BankService
@inject INotificationService NotificationService
@inject IStringLocalizer<SharedResource> L

<GenericEditModalComponent TEntity="Bank"
                          TService="IBankService"
                          @ref="editModalComponent"
                          IsVisible="@IsVisible"
                          IsVisibleChanged="@IsVisibleChanged"
                          @bind-Id="@BankId"
                          Service="@BankService"
                          EntityName="@L["Entity.Bank"]"
                          EntityNamePlural="@L["Entity.Bank"]"
                          ModalTitle="@(BankId.HasValue ? L["Message.EditTitle", L["Entity.Bank"]].ToString() : L["Message.CreateTitle", L["Entity.Bank"]].ToString())"
                          Size="GenericEditModalComponent<Bank, IBankService>.ModalSize.Desktop"
                          UseGenericForm="true"
                          FormFields="@formFields"
                          FormSections="@formSections"
                          DataLoader="@LoadBankData"
                          UseGenericSave="true"
                          SaveSuccessMessage="@(BankId.HasValue ? L["Message.UpdateSuccess", L["Entity.Bank"]].ToString() : L["Message.CreateSuccess", L["Entity.Bank"]].ToString())"
                          SaveFailureMessage="@L["Message.SaveFailure", L["Entity.Bank"]]"
                          RequiredPermission="Bank.Read"
                          OnEntitySaved="@OnBankSaved"
                          OnCancel="@OnCancel" />

@code {
    // ===== 參數 =====
    [Parameter] public bool IsVisible { get; set; } = false;
    [Parameter] public EventCallback<bool> IsVisibleChanged { get; set; }
    [Parameter] public int? BankId { get; set; }
    [Parameter] public EventCallback<Bank> OnBankSaved { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }
    [Parameter] public Dictionary<string, object?>? PrefilledValues { get; set; }

    // ===== 內部狀態 =====
    private GenericEditModalComponent<Bank, IBankService>? editModalComponent;
    private List<FormFieldDefinition> formFields = new();
    private Dictionary<string, string> formSections = new();

    // ===== 生命週期 =====
    protected override void OnParametersSet()
    {
        if (IsVisible)
        {
            InitializeFormFields();
        }
    }
    private Task<Bank?> LoadBankData()
        => EditDataLoaderHelper.LoadOrCreateAsync<Bank, IBankService>(
            BankId, BankService, NotificationService, "銀行", "BANK", PrefilledValues,
            b => b.BankName = string.Empty);
    // ===== 表單初始化 =====
    private void InitializeFormFields()
    {
        try
        {
            formFields = new List<FormFieldDefinition>
            {
                new ()
                {
                    PropertyName = nameof(Bank.Code),
                    Label = L["Field.BankCode"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.BankCode"]],
                    IsRequired = true,
                    HelpText = "銀行的唯一識別編號，新增時系統會自動產生，也可手動修改"
                },
                // 基本資訊區段
                new()
                {
                    PropertyName = nameof(Bank.BankName),
                    Label = L["Field.BankName"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.BankName"]],
                    IsRequired = true,
                    HelpText = "銀行的正式中文名稱"
                },
                new()
                {
                    PropertyName = nameof(Bank.BankNameEn),
                    Label = L["Field.EnglishName"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.EnglishName"]],
                    IsRequired = false,
                    HelpText = "銀行的英文名稱"
                },
                new()
                {
                    PropertyName = nameof(Bank.SwiftCode),
                    Label = L["Field.SwiftCode"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.SwiftCode"]],
                    IsRequired = false,
                    HelpText = "國際銀行匯款使用的SWIFT編號"
                },
                
                // 聯絡資訊區段
                new()
                {
                    PropertyName = nameof(Bank.Phone),
                    Label = L["Field.Phone"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.Phone"]],
                    IsRequired = false,
                    HelpText = "銀行的聯絡電話"
                },
                new()
                {
                    PropertyName = nameof(Bank.Fax),
                    Label = L["Field.Fax"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.Fax"]],
                    IsRequired = false,
                    HelpText = "銀行的傳真號碼"
                },
                new()
                {
                    PropertyName = nameof(Bank.Address),
                    Label = L["Field.Address"],
                    FieldType = FormFieldType.Text,
                    Placeholder = L["Placeholder.PleaseInput", L["Field.Address"]],
                    IsRequired = false,
                    HelpText = "銀行的地址",
                    ContainerCssClass = "col-6"
                },
                FormFieldConfigurationHelper.CreateRemarksField<Bank>()
            };

            formSections = FormSectionHelper<Bank>.Create()
                .AddToSection(FormSectionNames.BasicInfo,
                    b => b.Code,
                    b => b.BankName,
                    b => b.BankNameEn,
                    b => b.SwiftCode)
                .AddToSection(FormSectionNames.ContactInfo,
                    b => b.Phone,
                    b => b.Fax,
                    b => b.Address)
                .AddToSection(FormSectionNames.AdditionalData,
                    b => b.Remarks)
                .Build();
        }
        catch (Exception)
        {
            _ = NotificationService.ShowErrorAsync("初始化表單欄位時發生錯誤");
        }
    }

    // ===== 公開方法 (供父組件調用) =====
    public Task ShowAddModal() => editModalComponent!.ShowAddModal();
    public Task ShowEditModal(int id) => editModalComponent!.ShowEditModal(id);
}
