@* 可重用的商品類型編輯組件 - 可在任何頁面中嵌入 *@
@inject IProductCategoryService ProductCategoryService
@inject INotificationService NotificationService
@inject IStringLocalizer<SharedResource> L

<GenericEditModalComponent TEntity="ProductCategory"
                          TService="IProductCategoryService"
                          @ref="editModalComponent"
                          IsVisible="@IsVisible"
                          IsVisibleChanged="@IsVisibleChanged"
                          @bind-Id="@ProductCategoryId"
                          Service="@ProductCategoryService"
                          EntityName="@L["Entity.ProductCategory"]"
                          EntityNamePlural="@L["Entity.ProductCategory"]"
                          ModalTitle="@(ProductCategoryId.HasValue ? L["Message.EditTitle", L["Entity.ProductCategory"]].ToString() : L["Message.CreateTitle", L["Entity.ProductCategory"]].ToString())"
                          Size="GenericEditModalComponent<ProductCategory, IProductCategoryService>.ModalSize.Desktop"
                          UseGenericForm="true"
                          FormFields="@formFields"
                          FormSections="@formSections"
                          DataLoader="@LoadProductCategoryData"
                          UseGenericSave="true"
                          SaveSuccessMessage="@(ProductCategoryId.HasValue ? L["Message.UpdateSuccess", L["Entity.ProductCategory"]].ToString() : L["Message.CreateSuccess", L["Entity.ProductCategory"]].ToString())"
                          SaveFailureMessage="@L["Message.SaveFailure", L["Entity.ProductCategory"]]"
                          RequiredPermission="@PermissionRegistry.ProductCategory.Read"
                          OnEntitySaved="@OnProductCategorySaved"
                          OnCancel="@OnCancel" />

@code {
    // ===== 必要參數 =====
    [Parameter] public bool IsVisible { get; set; } = false;
    [Parameter] public EventCallback<bool> IsVisibleChanged { get; set; }
    [Parameter] public int? ProductCategoryId { get; set; }
    [Parameter] public EventCallback<ProductCategory> OnProductCategorySaved { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }
    [Parameter] public Dictionary<string, object?>? PrefilledValues { get; set; }

    // ===== 內部狀態 =====
    private GenericEditModalComponent<ProductCategory, IProductCategoryService>? editModalComponent;
    private List<FormFieldDefinition> formFields = new();
    private Dictionary<string, string> formSections = new();

    // ===== 必要方法 =====
    private Task<ProductCategory?> LoadProductCategoryData()
        => EditDataLoaderHelper.LoadOrCreateAsync<ProductCategory, IProductCategoryService>(
            ProductCategoryId, ProductCategoryService, NotificationService, "商品類型", "PCAT", PrefilledValues,
            p => p.Name = string.Empty);

    protected override void OnParametersSet()
    {
        if (IsVisible)
        {
            InitializeFormFields();
        }
    }

    private void InitializeFormFields()
    {
        formFields = new List<FormFieldDefinition>
        {
            new()
            {
                PropertyName = nameof(ProductCategory.Code),
                Label = L["Field.CategoryCode"],
                FieldType = FormFieldType.Text,
                Placeholder = L["Placeholder.PleaseInput", L["Field.CategoryCode"]],
                IsRequired = false,
                MaxLength = 50,
                HelpText = "商品分類的唯一編號，新增時系統會自動產生，也可手動修改"
            },
            new()
            {
                PropertyName = nameof(ProductCategory.Name),
                Label = L["Field.CategoryName"],
                FieldType = FormFieldType.Text,
                Placeholder = L["Placeholder.PleaseInput", L["Field.CategoryName"]],
                IsRequired = true,
                MaxLength = 50,
                HelpText = "商品分類的名稱，用於區分不同的商品類型"
            },
            new FormFieldDefinition()
            {
                PropertyName = nameof(ProductCategory.IsSaleable),
                Label = L["Field.ForSale"],
                FieldType = FormFieldType.Checkbox,
                IsRequired = false,
                HelpText = "指示此商品分類下的商品是否可供銷售"
            },

            FormFieldConfigurationHelper.CreateRemarksField<ProductCategory>()
        };

        formSections = FormSectionHelper<ProductCategory>.Create()
            .AddToSection(FormSectionNames.BasicInfo,
                pc => pc.Code,
                pc => pc.Name,
                pc => pc.IsSaleable)
            .AddToSection(FormSectionNames.AdditionalInfo,
                pc => pc.Remarks)
            .Build();
    }
}